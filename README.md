#  cloud-1

<insert description>

## Goal

## Notes

We need the following :

python
docker


A ansible.cfg file allows us to customize the ansible configuration for a specific project (SSH connection, inventory, privilegies ...). It is written in the INI format.


command to launch a playbook on localhost :
```
bash 

ansible-playbook playbooks/startDocker.yaml --connection=local

```
## Resources
